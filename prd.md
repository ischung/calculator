# 4칙 연산 계산기 PRD

**작성일**: 2026-03-10 | **버전**: v0.1 | **상태**: Draft

---

## 1. 프로젝트 개요

### 1.1 Core Goal
기존 계산기 예제의 밋밋한 UI 문제를 해결하여, 첫눈에 감탄을 이끌어내는 4칙 연산 시연용 계산기를 제공한다.

### 1.2 배경 및 목적
- **대상 사용자:** 개발자/교육자 (포트폴리오 또는 수업 시연용)
- **사용자 Pain Point:** 기존 계산기 예제들이 UI도 투박하고 코드도 복잡하게 얽혀 있어 수업 시연이나 포트폴리오에 활용하기 부끄럽다
- **기대 효과:** 시연 시 "이게 계산기야?" 반응이 나올 만큼 완성도 높은 UI 제공

### 1.3 범위 (Scope)

**In-Scope (v1 포함)**
- 숫자 버튼 (0~9)
- 4칙 연산 버튼 (+, -, ×, ÷)
- 계산 실행 버튼 (=)
- 전체 초기화 버튼 (C)
- 입력값/결과 표시 화면 (디스플레이)
- 소수점 입력 버튼 (.)
- 직전 입력 삭제 버튼 (⌫)

**Out-of-Scope (v1 제외)**
- 계산 히스토리 (이유: 핵심 흐름 외)
- 괄호 연산 (이유: 4칙 연산 범위 초과)
- 공학용/함수 연산 (이유: 4칙 연산 범위 초과)
- 단위 변환 (이유: 4칙 연산 범위 초과)

---

## 2. 대상 사용자

**Primary Persona**: 박교수, 42세, 소프트웨어공학 강의자. 수업 첫날 UI/UX의 중요성을 계산기로 시연하고 싶다.

**핵심 니즈**:
- 기능적: 수업 중 오류 없이 안정적으로 동작하는 계산기
- 감성적: 학생들에게 "좋은 UI가 이런 것"을 눈으로 보여주고 싶다

---

## 3. 유저 스토리

| 우선순위 | 스토리 |
|----------|--------|
| P0 | 박교수는 투박한 기본 예제 대신 완성도 높은 계산기를 보여줌으로써, 학생들이 UI/UX 설계의 가치를 체감하게 한다. |

---

## 4. 핵심 성과 지표 (KPIs)

| 지표명 | 목표 수치 |
|--------|-----------|
| GitHub Star 수 | 출시 1개월 후 50개 이상 |
| 수업 시연 후 학생 만족도 | 설문 응답 80% 이상 긍정 반응 |

---

## 5. 상세 기능 요건

### 5.1 숫자 버튼 (0~9)

**Logic**:
1. 숫자 버튼을 누르면 디스플레이에 이어서 표시된다
2. 연산자 버튼을 누른 직후라면 새로운 숫자 입력이 시작된다
3. `0`으로 시작하는 중복 0 입력을 방지한다

**Validation**: 자릿수 제한 없음

**Edge Case**:
- 데이터 없음: 초기 상태에서 디스플레이에 `0` 표시
- 오류 발생: 디스플레이에 `"앗, 0으로 나눌 수 없어요!"` 표시, C 버튼으로 초기화 가능

### 5.2 4칙 연산 버튼 (+, -, ×, ÷)

**Logic**:
1. 숫자 입력 후 연산자 버튼을 누르면 수식이 이어진다
2. 연산자를 연속으로 누르면 마지막에 누른 연산자로 교체된다
3. `=` 버튼을 누르면 계산 결과가 표시된다

**Validation**: 숫자 입력 없이 연산자만 누르면 무시한다 (단, `-`는 음수 부호로 허용)

**Edge Case**:
- 데이터 없음: 연산자만 누르고 두 번째 숫자 없이 `=` 누르면 첫 번째 숫자를 그대로 결과로 표시
- 오류 발생: 디스플레이에 `"앗, 0으로 나눌 수 없어요!"` 표시, C 버튼으로 초기화

### 5.3 계산 실행 버튼 (=)

**Logic**:
1. `=` 버튼을 누르면 입력된 수식을 계산하여 결과를 표시한다
2. 결과 표시 후 연산자를 누르면 결과값을 첫 번째 숫자로 이어서 계산한다
3. 결과 표시 후 숫자를 누르면 새로운 계산을 시작한다
4. `=`을 연속으로 누르면 마지막 연산을 반복한다 (예: `5 + 3 = = =` → `11, 14`)

**Validation**: 연산자 없이 `=`만 누르면 무시한다

**Edge Case**:
- 데이터 없음: 아무것도 입력 안 한 상태에서 `=` 누르면 무시, 디스플레이 `0` 유지
- 오류 발생: 0으로 나누기 후 `=` 누르면 `"앗, 0으로 나눌 수 없어요!"` 표시

### 5.4 전체 초기화 버튼 (C)

**Logic**:
1. `C` 버튼을 누르면 입력값, 연산자, 결과값이 모두 초기화된다
2. 디스플레이가 `0`으로 돌아간다

**Validation**: 초기 상태(`0` 표시 중)에서는 `C` 버튼을 눌러도 무시한다

**Edge Case**:
- 데이터 없음: 초기 상태에서 `C` 누르면 무시
- 오류 발생: `Error` 상태에서 `C` 누르면 초기화하고 `0`으로 복귀

### 5.5 디스플레이 (입력값/결과 표시 화면)

**Logic**:
1. 현재 입력 중인 숫자 또는 계산 결과를 표시한다
2. 숫자가 길어지면 글자 크기를 자동으로 줄여 한 줄에 표시한다
3. 연산자 입력 시 수식을 작게, 현재 입력값/결과를 크게 표시한다

### 5.6 소수점 버튼 (.)

**Logic**:
1. `.` 버튼을 누르면 현재 입력 중인 숫자에 소수점이 추가된다
2. 이미 소수점이 있으면 추가 입력을 무시한다
3. 연산자 직후 `.`을 누르면 `0.`으로 자동 시작한다

### 5.7 직전 입력 삭제 버튼 (⌫)

**Logic**:
1. `⌫` 버튼을 누르면 현재 입력 중인 숫자의 마지막 자리가 삭제된다
2. 숫자가 한 자리일 때 누르면 `0`으로 돌아간다
3. 계산 결과 표시 중에는 `⌫`을 무시한다 (`C`로만 초기화 가능)

### 5.8 인터랙션

| 사용자 행동 | 시스템 반응 |
|------------|------------|
| 숫자 버튼 클릭 | 디스플레이에 숫자 이어서 표시 |
| 연산자 버튼 클릭 | 연산자 교체 또는 수식 이어가기 |
| `=` 클릭 | 계산 결과 표시 |
| `C` 클릭 | 전체 초기화 → `0` |
| `⌫` 클릭 | 마지막 자리 삭제 |
| `.` 클릭 | 소수점 추가 (중복 무시) |
| 0으로 나누기 | `"앗, 0으로 나눌 수 없어요!"` 표시 |

---

## 6. UI/UX 요건

- **레이아웃:** 카드형 — 배경과 대비되는 둥근 카드가 중앙에 떠있는 느낌, 그림자 효과
- **버튼 피드백:** 클릭 시 살짝 눌리는 애니메이션 효과 (scale down)
- **오류 메시지 톤:** 친근하고 가볍게 (`"앗, 0으로 나눌 수 없어요!"`)

---

## 7. 기술적 제약

- **지원 환경:** 데스크탑 앱
- **성능 목표:** TBD
